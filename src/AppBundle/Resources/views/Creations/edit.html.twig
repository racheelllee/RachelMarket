{% extends "layout.html.twig" %}
	{% block title %} Create New Creation {% endblock %}
	{% block body %}
		{{ form_start(form, {'action':path('creations.update', { 'id' : id }), 'method':'PUT'}) }}
		    {{ form_errors(form) }}
		    <input type="submit" value="Editar">
		{{ form_end(form) }}
	{% endblock %}